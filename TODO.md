# 向涛面试准备清单

> 写于 2025-07-05，基于真实的模拟面试反馈。
> 两个项目都是 AI 辅助开发，技术深度不够，业务体感没有。
> 目标是能通过中厂的 Java 后端初面。

---

## 一、要背的：项目面试话术（P0 — 最急，1 周内搞定）

### 1.1 AI 面试官项目（你最有可能拿分的项目）

这 7 个问题面试必问。用自己的话讲顺，不是背，是讲你真实遇到的事。

---

**Q1: "这个项目是做什么的？用了什么技术栈？"**

技术栈：Python + Streamlit + LangChain + Chroma + 智谱 GLM-4 + Embedding-2

面试官把"出题者 + 评估者 + 主持人"三个角色拆成了 Agent 的四个 Tool：
- search_question_bank（RAG 检索）
- evaluate_answer（内部评估，temp=0.1）
- adjust_difficulty（调整难度）
- generate_final_report（最终报告）

核心链路：简历 → 解析 → Chroma 向量检索 → 智谱 LLM 出题 → 用户回答 → Agent 内部评估 → 追问或下一题 → 面试结束 → 生成结构化报告

---

**Q2: "为什么用 LangChain？"**

LLM + RAG + Agent 这套组合 LangChain 抽象最成熟。但我踩过坑——ChatPromptTemplate.format() 在不同版本行为不一样，调试花了不少时间。另外 LangChain 的 AgentExecutor 每次 invoke 是无状态的，我得手动把对话历史和进度通过 input 传进去。

---

**Q3: "为什么用 Chroma 而不是别的向量数据库？"**

Chroma 部署极简——pip install 就行，不需要额外服务。适合中小规模 RAG。但如果是生产环境大规模部署，会考虑 Milvus（性能更强）或 Pinecone（全托管）。

---

**Q4: "为什么用智谱 Embedding 而不是本地模型？"**

国内网络问题。HuggingFace 被墙，`all-MiniLM-L6-v2` 下载不下来，试了镜像 hf-mirror.com 也不稳定。换成智谱 Embedding API 后网络稳定，不需要本地加载 90MB 模型文件。代价是按量付费（百万 token 几毛钱）。

---

**Q5: "chunk_size 为什么设 32？"**

智谱 Embedding API 限制每次最多 64 条输入。200 道题如果一次性发过去直接 400 报错。设 32 留了余量——分 7 批（32+32+32+32+32+32+8）发完。

---

**Q6: "System Prompt 怎么设计的？"**

改了至少 5 版才调出比较自然的面试节奏。关键规则：
- 不知道就跳过（不纠缠）
- 内部评分不暴露
- 随机追问/换题（40%/60%）
- 追问最多 1 次
- 换题时用不同关键词搜索

---

**Q7: "做这个项目遇到的最大坑是什么？"**

三个坑：
1. 网络——HuggingFace 连不上，换智谱 API
2. API 限制——智谱一次最多 64 条，加了 chunk_size=32 分批
3. Agent 面试节奏——最初每次回答完直接亮评分，体验很差。后来把 evaluate_answer 改成了内部决策工具，System Prompt 加了"绝不透露评分"的硬约束

---

### 1.2 ChatRobot 项目（至少能说清楚）

时间不够的话弱化这个项目。如果面试官问了，至少说清：

- 架构：Vue 前端 + Java 后端 + LLM API
- 多轮对话上下文：滑动窗口保留最近 10 轮 + 摘要压缩
- 多用户隔离：session_id 存在 Redis 里，按用户区分
- 如果重做会改什么：消息队列用 RabbitMQ 代替 Redis List，做真正的异步处理

---

## 二、要背的：Java 八股文（P2 — 持续补）

时间有限，优先这 4 块。每块准备三层回答：
1. "是干什么的"（定义）
2. "你项目里怎么用的"（结合项目）
3. "出问题了怎么办"（排障故事）

### 2.1 Java 集合（高频 + 相对简单）

| 知识点 | 必答点 |
|--------|--------|
| HashMap 原理 | 数组+链表+红黑树、put/get 过程、扩容机制（负载因子 0.75） |
| ConcurrentHashMap | JDK7 分段锁 vs JDK8 CAS+synchronized、为什么线程安全 |
| ArrayList vs LinkedList | 数组 vs 双向链表、随机访问 vs 插入删除、各自复杂度 |

### 2.2 JVM（你简历写了"熟悉 JVM 调优"，问到的概率极高）

| 知识点 | 必答点 |
|--------|--------|
| 内存模型 | 堆、栈、元空间、程序计数器。堆分新生代（Eden+S0+S1）和老年代 |
| GC 算法 | 标记-清除、标记-整理、复制算法。分别用于哪里 |
| GC 回收器 | CMS（关注低延迟）vs G1（Region+可预测停顿）。JDK9 后默认 G1 |
| 一个对象的一生 | new → Eden → Minor GC → Survivor 来回 → 年龄到 15 → 老年代 → Major GC → 回收 |
| 类加载 | 双亲委派模型：Bootstrap→Extension→Application。为什么这样设计 |

### 2.3 Java 并发

| 知识点 | 必答点 |
|--------|--------|
| volatile | 可见性 + 禁止重排序。不能保证原子性。DCL 单例为什么必须加 volatile |
| synchronized 锁升级 | 偏向锁→轻量级锁（CAS 自旋）→重量级锁（OS 互斥）。为什么这样设计 |
| synchronized vs ReentrantLock | 前者 JVM 层面自动释放，后者需要 finally unlock。ReentrantLock 支持可中断、公平锁、Condition |
| 线程池 | corePoolSize、maxPoolSize、workQueue、拒绝策略四种 |
| ThreadLocal | 线程隔离、底层 ThreadLocalMap、内存泄漏原因 |

### 2.4 MySQL

| 知识点 | 必答点 |
|--------|--------|
| 索引 | B+Tree 结构、聚簇索引 vs 非聚簇索引、回表、覆盖索引、最左前缀原则 |
| 事务隔离级别 | 读未提交→读已提交→可重复读→串行化。脏读/不可重复读/幻读 |
| MVCC | 多版本并发控制、undo log、ReadView。InnoDB 怎么做到可重复读的 |
| 慢查询 | slow_query_log、EXPLAIN、type 字段（all/index/range/ref/const） |

---

## 三、要做的事：补业务体感（P1 — 2 周内）

### 3.1 把 AI 面试官项目真的部署起来

不管你用什么方式（自己电脑、云服务器、内网），让至少 2 个朋友真实用一下。你会遇到的真实问题：

1. **有人上传格式奇怪的简历**（50 页 PDF、全是图的简历、空文件）→ 你的解析函数怎么处理？
2. **智谱 API 突然限流/欠费** → 前端怎么提示用户？
3. **Chroma 数据库文件损坏** → 怎么恢复？怎么避免？
4. **有人疯狂点"开始面试"** → session_state 有没有竞态？

只要遇到并解决一个，面试时你就有真实故事了。面试官要的不是完美方案，是你排查问题的思路。

### 3.2 从零写一个小模块（1 个月，不急但重要）

目的：证明你不只会调 AI 写代码。

选一个：
- **手写一个 Embedding 调用**：不用 langchain_openai，直接用 requests 调智谱 Embedding API，拿到向量自己算余弦相似度
- **手写一个简单的线程池**：管理 LLM 并发调用，控制最大并发数
- **手写 LRU 缓存**：给 ChatRobot 的 LLM 调用加一层本地缓存

做完后你对底层原理的理解会上一个台阶。

---

## 四、面试当场：不会就说不会

这是最重要的建议——比背八股文重要。

**如果问到不会的：**

> ❌ "这个...嗯...我觉得..."（硬编，面试官一眼看穿）
> ✅ "这个我没有深入做过，但我的理解是...（简述思路）。如果让我去做，我会先查 XXX 文档，从 YYY 入手。"
> ✅ 像你刚才那样——"说实话这个我没真正用过，只在学习阶段了解了基本原理"

**面试官不怕你不会，怕你装懂。** 你能坦诚说"RabbitMQ 实际项目里我用的是 Redis List 做轻量级替代"，反而是加分——说明你知道技术选型不是堆砌技术栈。

---

## 五、快速自查清单

面试前一天过一遍：

- [ ] AI 面试官的 7 个问题能不能不看稿讲出来？
- [ ] HashMap、ConcurrentHashMap 能不能画图解释？
- [ ] volatile、synchronized 锁升级能不能用自己的话说一遍？
- [ ] 类加载双亲委派、GC 流程能不能讲给非技术朋友听懂？
- [ ] MySQL 索引 B+Tree 长什么样、为什么不用 B-Tree？
- [ ] 准备一个"我遇到的最大坑"的真实故事
- [ ] 准备一个"如果重做我会怎么改"的反思
- [ ] 准备一个"我不会但我打算怎么学"的回答模板

---

> 以上内容全部基于 2025-07-05 模拟面试的真实反馈。
> 有变化随时更新。
