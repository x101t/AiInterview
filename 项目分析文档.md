# Stage 1 产出：双项目分析文档

> 生成日期：2025-07-11
> 用途：理解项目现状 → 找到问题 → 规划改造

---

## 项目 A：AI 面试官（d:\Code\AIInterview）

### 1. 需求文档

| 编号 | 功能 | 描述 | 当前状态 |
|------|------|------|:--:|
| R1 | 简历解析 | 支持 PDF / Word / TXT 上传，提取纯文本 | ✅ 已实现 |
| R2 | 题库向量检索 | 200 道面试题存入 Chroma，按简历语义检索 | ✅ 已实现 |
| R3 | Agent 出题 | LLM 根据简历 + 题库检索结果，选一道题来问 | ✅ 已实现 |
| R4 | 追问机制 | 回答好→追问；不知道→换题 | ✅ 已实现 |
| R5 | 回答评估 | 内部评估（不透露评分），指导 Agent 下一步动作 | ✅ 已实现 |
| R6 | 难度调整 | 根据历史得分调整后续题目难度 | ⚠️ 有工具但未闭环 |
| R7 | 面试报告 | 面试结束生成结构化评估报告 | ✅ 已实现 |
| R8 | 多轮对话 | 记住上下文，不重复问同样的问题 | ⚠️ 部分实现 |

### 2. 接口清单（Agent 工具接口）

| 工具 | 输入 | 输出 | 调用方 |
|------|------|------|--------|
| `search_question_bank` | query, difficulty | 3 道匹配的题+答案 | Agent 出题时调用 |
| `evaluate_answer` | question, user_answer, reference | 评分+动作建议 | Agent 评估时调用 |
| `adjust_difficulty` | scores (如 "8,7,9") | easy/medium/hard | Agent 需要调整时调用 |
| `generate_final_report` | history (完整记录) | 结构化评估报告 | Agent 结束时调用 |

### 3. 当前代码问题清单（Bug / 缺陷）

#### 🔴 严重

| # | 位置 | 问题 | 影响 | 谁来修 |
|---|------|------|------|:--:|
| 1 | `app.py:265` | AgentExecutor 每次用户回答都重建—agent 在 session_state 存了但 `verbose=True` 每次都打日志 | 性能浪费 | 你 |
| 2 | `search_question_bank` | Chroma filter 传空 dict `{}` 时行为不确定—可能匹配到不属于任何难度的文档 | 检索不准 | 我 |
| 3 | 整体 | **完全没有单元测试**——200 行逻辑全靠手动点了测 | AI 生成项目的通病 | 你写 |
| 4 | `parse_resume` | 50 页 PDF / 空文件 / 图片型 PDF 没有保护——直接崩 | 面试官视角的稳定性漏洞 | 你 |
| 5 | `init_vectorstore` | Embedding API 调用没有重试——智谱偶尔超时就挂了 | 生产不可用 | 你 |

#### 🟡 中等

| # | 位置 | 问题 | 影响 | 谁来修 |
|---|------|------|------|:--:|
| 6 | `evaluate_answer` | 评分 Prompt 只有三行输出要求，没有 JSON 格式约束——Agent 可能解析不了 | 偶尔失控 | 我 |
| 7 | `build_agent` | System Prompt 太长（~40 行），每次 invoke 都重传——浪费 token | Token 成本高 | 你 |
| 8 | `adjust_difficulty` | 工具返回了 difficulty，但 Agent 没把它传到下一个 `search_question_bank` 的调用里——**难度调整没闭环** | R6 功能形同虚设 | 你 |
| 9 | `.env` | `DEEPSEEK_KEY` 仍是占位符 `your-deepseek-key-here`，实际只用智谱 | 说明代码里有死配置 | 你 |

#### 🟢 小问题

| # | 位置 | 问题 | 影响 | 谁来修 |
|---|------|------|------|:--:|
| 10 | 全局 | `os.getenv()` 散落在各处，没有集中管理配置 | 维护性差 | 你 |
| 11 | `search_question_bank:108` | 参数名拼写 `difficult`（应为 `difficulty`） | Agent 可能不识别这个参数名 | 我 |
| 12 | 全局 | 大量 AI 生成的冗余注释未清理 | 面试官一眼看出是 AI 写的 | 你 |

### 4. 🖐 你必须手写的内容

> 以下标"你写"的不要让我生成。写完了贴给我，我帮你 review。

1. **`parse_resume` 的边界测试用例**（你写 5 条）：
   - 空文件怎么处理？
   - 50 页 PDF 怎么处理？
   - 图片型 PDF 怎么处理？
   - 文件名为中文怎么处理？
   - 损坏的 docx 怎么处理？
   
2. **`evaluate_answer` 的评分稳定性测试**（你写 3 条）：
   - "不知道"的回答应该出什么结果？
   - 同一段回答调 3 次，评分方差应该多少以内？
   - 超长回答（2000 字）会不会崩？

3. **`init_vectorstore` 的重试逻辑**（你写代码）

4. **`adjust_difficulty` 闭环逻辑**（你写代码）：
   - Agent 评估完后怎么把 score 存下来？
   - 下次调 `search_question_bank` 时怎么把当前难度传进去？

---

## 项目 B：RAG 智能客服 chat_robot（D:\AI_Projects\chat_robot）

### 1. 需求文档

| 编号 | 功能 | 描述 | 当前状态 |
|------|------|------|:--:|
| R1 | AI 对话 | 用户发消息→DeepSeek 回复，支持多轮上下文 | ✅ 已实现 |
| R2 | 知识库匹配 | 先查精确匹配（SQL LIKE），未命中再走 RAG 向量检索 | ✅ 已实现 |
| R3 | 向量检索 | **自实现** TF-IDF + 余弦相似度（无需外部分词器！） | ✅ 已实现 |
| R4 | Function Calling | 工具系统（天气、快递）+ Agent 循环（最多 3 轮） | ✅ 已实现 |
| R5 | 转人工 | AI 无法回答时自动标记转人工 | ✅ 已实现 |
| R6 | 知识库管理 | 管理后台 CRUD 知识条目 | ✅ 已实现 |
| R7 | 会话管理 | 多会话、消息历史、状态流转 | ✅ 已实现 |
| R8 | JWT 认证 | 用户登录、token 鉴权、拦截器 | ✅ 已实现 |
| R9 | 三端前端 | 管理后台（Vue3）+ 用户端（Vue3）+ 小程序（微信原生） | ✅ 已实现 |
| R10 | 数据分析 | 统计图表、热门问题 TOP10 | ⚠️ README 写了但标注"占位" |

### 2. API 接口清单

#### 后端 API（`robot/src/main/java/com/chatbot/controller/`）

| 方法 | 路径 | 功能 | 权限 |
|------|------|------|:--:|
| POST | `/api/auth/login` | 管理员登录 | 公开 |
| POST | `/api/chat/send` | 发送消息（核心） | JWT |
| POST | `/api/chat/transfer` | 转人工 | JWT |
| GET | `/api/knowledge` | 知识库列表 | JWT |
| GET | `/api/knowledge/{id}` | 知识详情 | JWT |
| POST | `/api/knowledge` | 新增知识 | JWT |
| PUT | `/api/knowledge/{id}` | 更新知识 | JWT |
| DELETE | `/api/knowledge/{id}` | 删除知识 | JWT |
| GET | `/api/session/list` | 会话列表 | JWT |
| GET | `/api/session/{id}` | 会话详情 | JWT |

#### Agent 工具接口

| 工具 | 功能 | 注册位置 |
|------|------|---------|
| WeatherTool | 天气查询（调 wttr.in） | `service/tool/WeatherTool.java` |
| ExpressTool | 快递查询（调快递鸟 API） | `service/tool/ExpressTool.java` |

### 3. 🔴 安全问题（立即处理）

| # | 位置 | 问题 | 严重度 |
|---|------|------|:--:|
| 1 | `robot/README.md:177` | **DeepSeek API Key 明文暴露** `sk-5fd278d519b8464e979b98f1f84f5bb2` | 🔴 泄露 |
| 2 | `application.yml:42` | **另一把 DeepSeek API Key 明文暴露** `sk-09311a81e6c14798902d74fc7490beb7` | 🔴 泄露 |
| 3 | `application.yml:12` | 数据库密码 `root` 硬编码 | 🟡 |
| 4 | `application.yml:37` | JWT secret 硬编码 `chatbot-secret-key-2026-...` | 🟡 |

> ⚠️ **立即行动**：去 DeepSeek 后台把这两把 Key 全部 revoke（撤销），生成新的。README 和 yml 里的 Key 已经暴露在我能看到的代码里了，很可能已经泄露。

### 4. 当前代码问题清单

#### 🔴 严重

| # | 位置 | 问题 | 影响 | 谁来修 |
|---|------|------|------|:--:|
| 1 | `ChatController` | **完全没有参数校验**——`sessionId` 为空直接 NPE | 线上崩 | 你 |
| 2 | `ChatServiceImpl:207` | LLM 调用失败只打日志返回"抱歉"，**无重试机制** | 偶发失败丢失用户消息 | 你 |
| 3 | `VectorStoreService` | **纯内存存储**——服务重启所有向量数据丢失 | 重启即失忆 | 你 |
| 4 | `ChatServiceImpl:364-381` | `searchKnowledge` 用 SQL `LIKE` 模糊匹配——"怎么退款"和"退款"匹配不上，且 SQL 注入风险 | 检索召回率极低 | 你 |
| 5 | 全部 Controller | 无 `@Valid`、无 `@NotNull`、无全局异常处理器 | 非法输入直接 500 | 你 |
| 6 | 全局 | **零单元测试** | AI 生成项目的通病 | 你写 |

#### 🟡 中等

| # | 位置 | 问题 | 影响 | 谁来修 |
|---|------|------|------|:--:|
| 7 | `ChatServiceImpl:233` | 历史消息 LIMIT 5 硬编码——第六轮对话丢失上下文 | 长对话语义漂移 | 你 |
| 8 | `ChatServiceImpl:121-125` | 转人工判断逻辑太脆弱——纯靠关键词匹配"抱歉/无法/不能" | 误判率高 | 我 |
| 9 | `buildMessages:229` | System Prompt 每次都重建——没有缓存 | 性能浪费 | 你 |
| 10 | `KnowledgeController` | 无分页——知识库有 10000 条时直接 OOM | 不可用 | 你 |
| 11 | ChatController | 无限流——单用户可以疯狂发消息打挂后端 | 可被 DDoS | 你 |

#### 🟢 小问题 / 未实现功能

| # | 位置 | 问题 | 谁来修 |
|---|------|------|:--:|
| 12 | 全局 | 5 张截图 PNG 文件放在项目根目录——Git 仓库膨胀 | 你 |
| 13 | README | 标注 ⏳ 的功能（Excel 导入、数据分析图表等）都没实现 | 你 |
| 14 | `application.yml:18` | Redis Lettuce 连接池配了但没用——`ChatServiceImpl` 里根本没调 Redis | 你 |
| 15 | 全局 | AI 生成的大量中文注释 + 冗余空行未清理 | 你 |

### 5. 🖐 你必须手写的内容

1. **全局异常处理器**（你写）：
   - `@RestControllerAdvice` + `@ExceptionHandler`
   - 统一返回格式：`{"code": 500, "message": "...", "data": null}`
   - 至少覆盖：`MethodArgumentNotValidException`、`NullPointerException`、通用 `Exception`

2. **ChatController 参数校验**（你写）：
   - `ChatRequest` 里的字段全部加 `@NotBlank` / `@NotNull`
   - Controller 上加 `@Valid`

3. **LLM 调用重试逻辑**（你写）：
   - 在 `callDeepSeekRaw` 加 Retry 装饰器
   - 指数退避：1s → 2s → 4s，最多 3 次
   - 如果全失败 → 返回预设的降级回复

4. **知识库分页查询**（你写）：
   - KnowledgeController 的 list 接口加 `page` / `size` 参数
   - MyBatis Plus 的 `Page` 分页

5. **单元测试**（你写至少 5 条）：
   - `ChatServiceImpl.sendMessage`：正常消息返回非空
   - `ChatServiceImpl.sendMessage`：空消息（`""`）不崩
   - `ChatServiceImpl.sendMessage`：不存在的 sessionId 自动创建新 session
   - `VectorStoreService.search`：空库返回空列表不抛异常
   - `VectorStoreService.search`：加 1 条文档后能搜到

6. **VectorStoreService 持久化改造**（你写）：
   - 启动时从文件/SQLite 恢复向量
   - 新增文档时同步写盘
   - 最简单的方案：JSON 序列化写到本地文件

---

## 你和我各自做什么（明确分工）

### 我帮你做的（框架/模板/配置）
- 写 Dockerfile、docker-compose
- 写 README 模板（架构图占位，你填内容）
- 修改 Prompt / 参数名 / 小 bug
- Review 你写的代码

### 🖐 你必须手写的（标注在上方）
- 参数校验、全局异常处理 → 这是 Java 后端的基本功
- 单元测试 → 这是面试官判断"你会不会写代码"的核心证据
- 重试逻辑、分页 → 这是"Demo vs 工程"的分界线
- 边界测试用例 → 你需要培养"代码会在哪里崩"的直觉

### 你先做哪个？

按你的 4 周计划，**建议从 chat_robot 的手写任务开始**：
1. 先修安全：去 DeepSeek 后台 revoke Key，把配置移到 `.env`
2. 再补基本功：全局异常 + 参数校验（这是所有 Java 面试都会问的）
3. 再补测试：5 条单元测试
4. 再改架构：分页 + 重试 + VectorStore 持久化

AI 面试官的项目先不动，chat_robot 改完再说。
